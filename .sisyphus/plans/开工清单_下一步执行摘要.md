# 开工清单：下一步执行摘要

## 0. 先读这两份
1. `D:\vibe_coding\note\.sisyphus\plans\下一步构建指导_本地PostgreSQL与本地文件存储.md`
2. `D:\vibe_coding\note\docs\32_下一阶段AI构建详细任务清单.md`

这份清单只是开工摘要，细节、约束、验收标准以正式计划为准。

---

## 1. 当前已完成（不要重做）

### 后端已完成
- 用户/管理员双账号体系
- 文件记录链路
- ingestion
- parse jobs 查询/重试
- review tasks 列表/决策
- dashboard / admin monitor
- problems / notes 列表/详情/patch
- `/v1/solve`
- `/v1/search`
- `/v1/graph/overview`
- `/v1/graph/weak-tags`
- `/v1/admin/parse-jobs`

### 前端已完成
- 学生端比赛级界面
- 后台端比赛级界面
- 学生端已接登录 / 上传 / 内容列表 / 搜索 / 图谱概览
- 后台端已接登录 / 审核 / 监控 / parse jobs

### 远端已验证
- `root@146.190.84.189`
- smoke test 已通过主干接口

---

## 2. 技术决策（不要再改方向）

### 存储方案固定为
- **本地 PostgreSQL**：结构化数据
- **本地文件存储**：原始图片/PDF/doc/txt

### 本地文件目录
- Windows: `D:\vibe_coding\note\data\uploads`
- Linux: `/root/note/data/uploads`

### 文件路径规则
```text
data/uploads/{user_id}/{content_category}/{yyyy}/{mm}/{uuid}-{filename}
```

### 原则
- 文件本体不进数据库
- 数据库存文件元数据
- 不引入重型企业级架构
- 先做比赛级可用，再考虑商业化增强

---

## 3. 第一优先级：先做这 3 件事

### A. 真实 OpenAI 联调
必须完成：
- 在远端 `.env` 注入：
  - `OPENAI_API_KEY`
  - `OPENAI_BASE_URL`（如需要）
  - `OPENAI_MODEL_CLASSIFY`
  - `OPENAI_MODEL_SOLVE`
- 验证 classifier 走真实 OpenAI
- 验证 `/v1/solve` 走真实 OpenAI

完成标准：
- parse job 的 `llm_model` 有真实值
- `result_json` 不再是 mock
- `/v1/solve` 返回真实模型结果

### B. 本地文件存储落地
必须完成：
- 修改上传链路，改成真实本地路径生成
- `storage_provider` 语义转为 `local`
- `object_key` 存相对路径
- `file_url` 指向本地静态访问路径

完成标准：
- 上传图片/文档后，文件真实落到本地 `uploads` 目录
- 数据库里能查到相对路径与 URL

### C. 本地静态文件暴露
必须完成：
- FastAPI 挂载 `/uploads`
- 让本地文件可通过 URL 访问

完成标准：
- 上传文件后返回的 URL 可直接访问

---

## 4. 第二优先级：前端成品化

### 学生端
继续补：
- 上传进度/错误提示
- solve 结果展示区
- 更完整详情视图
- 搜索结果点击后的详情联动
- 图谱页或更完整图谱视图

### 后台端
继续补：
- parse jobs 筛选
- pending / failed / success 列表分组
- 失败任务重试入口

---

## 5. 第三优先级：部署固定化

### 必做
- 实测 `docker compose up -d`
- 确认 backend / worker / postgres / student / admin 都能起
- 选择一种长期运行方案：
  - `docker compose` 常驻
  - 或 `systemd`

### 后续建议
- 加 `nginx` / `caddy` 反向代理
- 固定 student / admin 的访问入口

---

## 6. 第四优先级：测试收口

### 现有脚本
- `D:\vibe_coding\note\scripts\smoke_check.py`

### 继续补
- solve smoke
- admin parse-jobs smoke
- upload classification smoke
- 本地文件可访问 smoke

---

## 7. 每完成一步必须做的事
1. 更新 `docs/` 下对应实现记录
2. 本地 build 或远端 smoke 验证
3. 记录失败原因和修复方式

---

## 8. 不要做的事
- 不要把文件存进数据库
- 不要删现有主干接口
- 不要重构成复杂微服务
- 不要把比赛级项目强行做成企业级系统

---

## 9. 开工第一步（直接照做）
如果现在马上开始执行，顺序如下：

1. 打开远端 `.env`
2. 填入真实 OpenAI 配置
3. 启动 backend
4. 验证 classifier 与 `/v1/solve`
5. 然后再改本地文件存储链路

---

## 10. 当前最重要的一句话
> 现在不要再重新规划，也不要再改架构方向。直接按“**真实 OpenAI 联调 → 本地文件存储落地 → 前端成品化 → 部署固定化**”推进。
