# 全量未完成事项盘点与下一步施工顺序

## 1. 文档目的

这份文档用于给当前执行者与下一位 AI 一个**统一且最新**的未完成事项总视图。

要求是：

1. 不再重复已经完成的工作；
2. 明确 backend / frontend / deploy / test / docs 五个维度还剩什么；
3. 给出可以直接继续施工的顺序；
4. 对每一类未完成项都给出代码证据或文档证据。

## 2. 已完成的大项（不要重做）

截至当前，以下主线已完成并已有代码/远端验证/文档证据：

1. 后端主干接口已完成；
2. 本地 PostgreSQL + 本地文件存储主线已落地；
3. 真实文件写盘闭环已完成；
4. `/uploads` 静态访问已完成；
5. 真实 AI 主链路联调已完成；
6. 关键 AI 失败路径已补充大部分证据；
7. 远端 compose 全链路实跑已完成；
8. worker 常驻化已完成；
9. `systemd + docker compose` 首轮固定化已完成；
10. student/admin 前端已可构建并已在远端部署验证。

## 3. 当前全量未完成事项（按领域拆分）

## 3.1 Backend / AI / 数据侧

### B1. AI 失败路径主缺口已基本补齐

**状态**：已从主未完成项降级为“可继续增强测试覆盖”。

**现有证据**：

- 缺 key → `5001`
- base URL 错误 / 超时 → `5002`
- 错误模型名 → `5002`
- classifier schema 不兼容 → `5005`
- solver non-JSON → `5008`
- classifier non-JSON → `5007`

**涉及文件**：

- `apps/backend/app/services/llm_client.py`
- `docs/61_真实AI失败路径回归验证记录.md`

### B2. PostgreSQL volume 与环境来源已完成首轮规范化

**状态**：已完成从匿名 volume 到命名卷 `note_postgres_data` 的迁移。

**当前结果**：

1. 远端 PostgreSQL 数据已迁移到命名卷 `note_postgres_data`；
2. `stargraph-compose.service` 已移除旧匿名 volume ID 依赖；
3. systemd 重启后 backend / worker / student / admin / postgres 全部正常恢复；
4. `/healthz`、`:3000`、`:3001` 与数据库表结构均已验证。

## 3.2 Frontend（学生端）

### F1. 学生端仍然是单页面板，不是完整产品流

**状态**：最小可演示，未成品化。

**当前代码证据**：

- 所有功能都集中在 `apps/frontend/student/src/App.vue`
- 无 router / route 结构
- 错题、笔记、搜索、图谱、solve、上传全部堆在同一页面

**仍缺**：

1. 更完整详情视图或路由化拆分；
2. 更清晰的信息层次与导航；
3. 避免用户在超大单页来回寻找功能区。

### F2. 学生端上传 UX 仍然粗糙，但本轮已完成最小一阶增强

**状态**：可上传，但交互非常基础。

**本轮新增完成**：

1. 已选文件信息摘要；
2. 图片文件预览；
3. 非图片文件占位预览卡片；
4. 页面内快速导航。

**当前代码证据**：

- `App.vue` 已补文件摘要与预览，但仍未形成完整上传体验
- 无拖拽区域
- 无进度条

**涉及文件**：

- `apps/frontend/student/src/App.vue`
- `apps/frontend/student/src/style.css`

### F3. 图谱展示仍然是概览，不是更完整图谱页

**状态**：已有可视化，但仍是轻量概览。

**当前代码证据**：

- `graphOverview` 只在当前单页中显示小型节点卡片
- 没有独立图谱视图
- 没有更深入的交互或筛选

### F4. 搜索与详情联动已有最小实现，但仍不够完整

**状态**：部分完成。

**当前代码证据**：

- `pickSearchResult()` 只是在当前内存列表里定位并切换选中项；
- 没有独立详情页跳转；
- 没有更强的上下文展示。

## 3.3 Frontend（后台端）

### A1. 后台 parse jobs 仍缺多维筛选与更强排障视图

**状态**：已有最小版，未成品化。

**当前代码证据**：

- `parseJobFilter` 只有单个 `status`
- `groupedParseJobs` 只按 pending/failed/success 做三组
- 任务详情仅靠整段 JSON `pre` 展示

**仍缺**：

1. 更复杂筛选组合；
3. 更成熟的任务详情/排障体验；
4. 更完整统计聚合。

**涉及文件**：

- `apps/frontend/admin/src/App.vue`
- `apps/frontend/admin/src/style.css`

## 3.4 Deploy / 运维

### D1. 公网入口 / 反向代理仍未完成

**状态**：服务器本机访问可用，公网入口未收口。

**当前现实**：

- backend: `127.0.0.1:8000`
- student: `127.0.0.1:3000`
- admin: `127.0.0.1:3001`

**仍缺**：

1. Nginx 或 Caddy；
2. 更清晰的访问入口；
3. 若需要，域名/公网映射文档。

### D2. 部署文档仍有历史时间差

**状态**：主要 docs 已更新，但仓库里仍有旧文档残留旧结论。

**证据**：

- `docs/53_本地文件存储链路实现记录.md`
- `docs/32_下一阶段AI构建详细任务清单.md`
- `docs/21_远程部署与验证记录.md`

这些文档仍需后续继续标记“旧状态已过期”。

## 3.5 测试 / QA

### T1. 已有可运行 regression 层，浏览器级 E2E 已完成最小首轮落地

**状态**：smoke 与 regression 已有，浏览器级 E2E 已新增首轮 student/admin 登录流，但整体覆盖仍明显不足。

**当前代码证据**：

- `scripts/smoke_check.py`
- `scripts/regression_check.py`
- `playwright.config.ts`
- `tests/e2e/student-auth.spec.ts`
- `tests/e2e/admin-auth.spec.ts`

**仍缺**：

1. 学生端上传 / 搜索 / solve / 内容详情 浏览器级 E2E；
2. 后台端 parse jobs / review actions 浏览器级 E2E；
3. 更系统的自动化回归体系。

## 4. 最推荐的继续施工顺序

基于当前代码现状与风险，最合理的顺序是：

1. 先做更深一层前端成品化；
2. 再把 regression 继续提升为浏览器级 E2E；
3. 公网入口 / 反向代理按用户要求暂不处理。

## 5. 当前最小高价值下一步

如果只做一个 backend 和一个 frontend 项，优先推荐：

### Test 下一步

- 在现有登录流 Playwright 基础上，继续补齐上传、搜索、solve 与后台任务流浏览器级 E2E。

### Frontend 下一步

- 继续把学生端从单页大面板收口成更清晰的详情/视图结构。

## 6. 对下一位 AI 的提醒

不要再重复做以下已完成事项：

1. compose 能不能跑起来；
2. worker 为什么会退出；
3. systemd 能不能拉起 compose；
4. 真实 AI 主链路是否已打通。

这些都已经有代码、远端验证和文档证据。
