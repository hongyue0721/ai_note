# 远程部署与验证记录

## 1. 远程目标

- 服务器：`root@146.190.84.189`

## 2. 当前状态

首次连通性验证结果：

- SSH 直连成功；
- 已确认该服务器可以作为后续真实运行与测试平台。

## 3. 计划用途

该服务器将承担：

1. 后端依赖安装验证；
2. 后端导入/启动校验；
3. 后续 Docker / 部署验证（若服务器具备对应环境）；
4. 最终比赛级成品联调验证。

## 4. 下一步

下一步读取服务器环境信息，确认 Python / Docker / 目录可用性，再同步项目到远端。

## 5. 已确认环境信息

远端探测结果：

- 系统：Ubuntu Linux
- Python：`3.12.3`
- 当前工作目录：`/root`
- Docker：已安装并可用（`/usr/bin/docker`，服务 active）

结论：

- 远端可直接作为 Python 运行级验证环境；
- 远端已具备继续推进 Docker 验证的基础条件。

## 6. 正在执行

正在将 `D:\vibe_coding\note` 同步到远端 `/root/note`，用于后续真实依赖安装与启动校验。

## 7. 用户新增授权

用户已明确授权：

- 缺少依赖可直接安装；
- 缺少 Docker 也可直接安装。

因此后续远端环境准备将包括：

1. 补齐 Python 依赖安装链路；
2. 安装 Docker；
3. 继续推进项目同步与运行验证。

## 8. 最新进展

已确认：

- Docker 已安装；
- Docker 服务已处于 active；
- 远端现在同时可承担 Python 运行验证与后续 Docker 验证。

当前阻塞已转为：

- 需要把本地 `note` 工程稳定同步到远端。

## 9. 同步策略调整

首次整目录 `scp -r` 传输因超时中断，判断原因更可能是：

- 项目目录中包含较大体积内容；
- 包含本地虚拟环境目录，不适合整体直接传输。

因此已调整为：

1. 远端重建 `/root/note`；
2. 本地打包源码与文档；
3. 排除 `.venv`、`__pycache__` 等无须同步内容；
4. 通过压缩流直接传到远端解包。

## 10. 远端 Python 环境进展

已在远端执行：

1. `python3 -m venv .venv`
2. 升级 pip
3. `pip install -r requirements.txt`

结果：

- 后端 Python 依赖已在远端成功安装；
- 这也侧面确认：当前代码在 Linux Python 3.12 平台上具备继续验证的基础；
- 随后进入顺序化导入校验与启动校验。

## 11. 2026-03-13 远端双端继续验证结果

本轮已继续通过 `ssh root@146.190.84.189` 在远端直接验证：

1. 同步本地最新 backend 改动到 `/root/note`；
2. 远端 `py_compile` 校验通过：
   - `apps/backend/app/core/config.py`
   - `apps/backend/app/api/routes/files.py`
   - `apps/backend/app/main.py`
3. 远端配置已切换为：
   - `STORAGE_PROVIDER=local`
   - `UPLOADS_ROOT_DIR=data/uploads`
   - `UPLOADS_URL_BASE=/uploads`
4. 远端实测接口通过：
   - `GET /healthz`
   - `POST /v1/auth/login`
   - `GET /v1/me`
   - `GET /v1/problems`
   - `GET /v1/notes`
   - `GET /v1/search`
   - `GET /v1/graph/overview`
   - `GET /v1/graph/weak-tags`
   - `POST /v1/files/upload-policy`
   - `POST /v1/files/confirm`

## 12. 远端上传链路验证结论

已确认：

1. `upload-policy` 返回的 `object_key` 已符合本地文件存储语义；
2. 返回的 `object_key` 已为相对路径，不再是旧的 `/dev/...` 风格；
3. `confirm` 返回的 `file_url` 已为 `/uploads/{object_key}`；
4. FastAPI 已成功挂载 `/uploads` 静态目录。

远端实测样例（摘要）：

```text
object_key = d734.../problem/2026/03/<uuid>-math-note.png
file_url   = /uploads/d734.../problem/2026/03/<uuid>-math-note.png
```

## 13. 远端静态文件暴露验证

已在远端手动写入：

```text
/root/note/data/uploads/manual-check/problem/2026/03/demo.txt
```

随后访问：

```text
http://127.0.0.1:8000/uploads/manual-check/problem/2026/03/demo.txt
```

返回结果：

- HTTP `200`
- 响应内容：`hello-static-check`

说明 `/uploads` 静态暴露链路已在远端验证通过。

## 14. 当前仍未完成的远端事项

当前还未完成：

1. 真实 OpenAI key / model 的联调验证；
2. 基于新本地文件存储语义的 smoke 脚本扩展；
3. `docker compose up -d` 的完整远端固定化；
4. student/admin 前端长期运行入口的固定化。

## 15. 备注

本轮还发现：

- 历史 `backend.log` 中保留过一次旧的 `passlib+bcrypt` 启动失败日志；
- 但当前远端实际运行的安全方案已是 `pbkdf2_sha256`，并且最新启动验证已成功；
- 因此后续排障应优先参考最新验证结果，不要被旧日志误导。
