# 阶段 8：双前端实现记录

## 1. 本次目标

在 `apps/frontend` 下真正建立两个可运行的前端项目：

1. 学生端 `student`
2. 后台端 `admin`

## 2. 本次已完成

### 2.1 脚手架初始化

已创建：

- `apps/frontend/student`
- `apps/frontend/admin`

技术栈：

- Vue 3
- TypeScript
- Vite

### 2.2 依赖安装

两个前端项目均已完成 `npm install`。

## 3. 当前阶段目标

下一步不再停留在模板，而是直接落：

### 学生端

- 登录页
- Dashboard
- 上传创建任务页
- 最近任务状态区
- 基础图谱展示区

### 后台端

- 管理员登录页
- 监控总览页
- 审核任务页
- 解析任务页

## 4. 下一步

下一步直接用真实后端接口驱动页面，做出比赛级可演示界面。

## 5. 本次新增实现

### 学生端

已完成：

1. 登录卡片
2. 上传与创建解析任务卡片
3. 最近任务状态区
4. 知识点可视化展示区

并且学生端已直接对接远端后端：

- `/v1/auth/login`
- `/v1/me`
- `/v1/files/upload-policy`
- `/v1/files/confirm`
- `/v1/ingestions`
- `/v1/parse-jobs/{id}`

### 后台端

已完成：

1. 管理员登录卡片
2. 监控总览面板
3. 审核任务列表
4. 审核决策按钮组

并且后台端已直接对接远端后端：

- `/v1/admin/auth/login`
- `/v1/review/tasks`
- `/v1/review/tasks/{id}/decision`
- `/v1/admin/monitor/overview`

## 6. 构建验证

已完成本地构建验证：

- `apps/frontend/student` 构建通过
- `apps/frontend/admin` 构建通过

说明两个前端项目当前都具备可打包能力。

## 7. 当前结论

双前端已不再是空模板，而是：

- 学生端：可展示真实学生链路
- 后台端：可展示真实审核与监控能力

已经达到比赛级成品展示面的基础标准。
