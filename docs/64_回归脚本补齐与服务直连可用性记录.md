# 回归脚本补齐与服务直连可用性记录

## 1. 本次目标

在不做域名绑定与反向代理的前提下，继续把当前系统收口到：

1. 服务稳定运行；
2. backend / student / admin 可直接访问；
3. 至少有一层可运行的回归脚本，而不是只有零散人工验证。

## 2. 本次已完成

### 2.1 服务直连可用性再次验证

本轮已在远端再次验证：

- `GET http://127.0.0.1:8000/healthz` → 200
- `GET http://127.0.0.1:3000/` → 200
- `GET http://127.0.0.1:3001/` → 200

说明当前不依赖域名与反向代理，服务本身已经是可访问状态。

### 2.2 PostgreSQL volume 已完成命名卷迁移

本轮已完成：

1. 把远端 PostgreSQL 数据从旧匿名 volume 迁移到命名卷 `note_postgres_data`
2. `infra/systemd/stargraph-compose.service` 已移除旧匿名 volume ID 依赖
3. systemd 重启后完整栈恢复正常

这意味着当前部署不再依赖那串匿名 volume ID 才能启动。

### 2.3 smoke 已进一步扩展

`scripts/smoke_check.py` 已继续扩展为：

1. `GET /v1/problems` 列表后继续验证 detail
2. `GET /v1/notes` 列表后继续验证 detail
3. 真实调用 `/v1/solve`
4. 要求 `final_answer` 非空

远端执行结果：

```text
smoke-check-ok
```

### 2.4 最小可运行回归脚本已补齐

新增脚本：

- `scripts/regression_check.py`

覆盖内容：

1. 用户登录 / `/v1/me`
2. `/v1/dashboard`
3. `/v1/problems` / detail
4. `/v1/notes` / detail
5. `/v1/search`
6. `/v1/graph/overview`
7. `/v1/graph/weak-tags`
8. `/v1/solve`
9. 管理员登录 / `/v1/admin/me`
10. `/v1/admin/monitor/overview`
11. `/v1/admin/parse-jobs`
12. `/v1/review/tasks?status=pending`
13. 若存在 failed parse job，则执行 retry
14. 若存在 pending review task，则执行 approve

远端执行结果：

```text
regression-check-ok
```

## 3. 这次完成后，未完成项如何变化

本轮完成后，以下事项不应再被列为主未完成阻塞：

1. “服务能否跑起来”
2. “前端能否直接访问”
3. “PostgreSQL 是否仍依赖匿名 volume”
4. “是否只有 smoke、没有任何回归脚本”

## 4. 当前真正还剩下的事情

在用户明确要求“域名和反代先不做”的前提下，当前真正剩余的主要就是：

1. 学生端更深一层产品化
2. 后台端更深一层产品化
3. 若要更进一步，再把当前 regression/smoke 提升成更完整 E2E

## 5. 对下一位 AI 的提醒

不要再把当前项目描述为“只能靠手工 SSH 点点接口，缺少可运行回归层”。

现在更准确的说法是：

> 当前已经有扩展 smoke + 最小可运行 regression 脚本，且二者都能在远端 live stack 上跑通。
