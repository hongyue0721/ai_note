# 最新未完成事项全量盘点（基于当前代码与验证）

## 1. 文档目的

这份文档用于在 **Playwright 首轮登录流 E2E 已落地** 之后，重新给出一份不依赖旧判断、直接基于当前代码与最新验证结果的未完成事项总表。

本文件优先级高于仓库内多数历史计划文档。若旧文档与本文件冲突，以本文件为准。

## 2. 已完成且不要重做的主线

以下事项已经完成，不应再被列为“当前主阻塞”：

1. 后端主干接口已完成；
2. 本地 PostgreSQL + 本地文件存储主线已完成；
3. `/uploads` 静态访问已完成；
4. 真实 AI 主链路已打通；
5. 主要 AI 失败路径已形成证据；
6. 远端 compose + systemd 固定化已完成首轮落地；
7. student/admin 前端可构建、可访问；
8. 浏览器级 E2E 已完成首轮登录流落地。

## 3. 当前仍未完成事项（按领域拆分）

## 3.1 Backend / AI / 数据层

### B1. AI 失败路径还没有充分沉淀为稳定脚本集

**状态**：部分完成。

**已完成**：

- key 缺失；
- base URL 错误 / timeout；
- 错误模型名；
- classifier schema 不兼容；
- solver non-JSON；
- classifier non-JSON。

**证据**：

- `docs/61_真实AI失败路径回归验证记录.md`
- `scripts/ai_failure_check.py`
- `apps/backend/app/services/llm_client.py`

**仍缺**：

1. 把更多失败路径沉淀成可重复执行的固定脚本或统一回归入口；
2. 避免失败路径验证继续只停留在“做过一次，有文档证据”。

### B2. solve_history / AI 历史记录层仍未实现

**状态**：未完成。

**证据**：

- `apps/backend/app/models/__init__.py` 当前没有 solve history 相关模型；
- 当前接口与前端也没有 AI 历史结果浏览能力。

**仍缺**：

1. 独立历史表；
2. 历史查询接口；
3. 前端历史浏览入口。

> 说明：这不是当前最优先项，但它仍然属于真实未完成事项，不能说已经做完。

## 3.2 Student Frontend

### F1. 学生端分层工作台已完成首轮落地，但仍未完全产品化

**状态**：部分完成。

**证据**：

- 登录层与工作台层已分离；
- 工作台已有上传笔记 / 询问问题 / 笔记管理 / 搜索 四个并列入口；
- 但仍是单个 `App.vue` 内的首轮结构，不是最终组件化成品。

**仍缺**：

1. 组件化拆分；
2. 更成熟的工作台信息层次；
3. 更强的详情/筛选/编辑体验。

### F2. 上传标签确认流已完成，但仍不成熟

**状态**：部分完成。

**已完成**：

- 文件选择；
- 图片预览；
- 非图片占位预览；
- 上传并发起解析。

**证据**：

- `apps/frontend/student/src/App.vue:454-514`

**仍缺**：

1. 拖拽上传；
2. 真正的上传进度条；
3. 更完整的失败恢复；
4. 问题图片提问链路和 solve 结果更深联动。

### F3. 图谱仍然只是概览区块

**状态**：部分完成。

**证据**：

- `apps/frontend/student/src/App.vue:546-574`

**仍缺**：

1. 独立图谱页；
2. 更深入节点交互；
3. 更强筛选与上下文联动。

### F4. 搜索与详情联动已做最小实现，但仍不完整

**状态**：部分完成。

**证据**：

- 搜索区：`apps/frontend/student/src/App.vue:576-603`
- detail 区：`apps/frontend/student/src/App.vue:647-705`
- 仅做内存列表联动，不是完整搜索结果页。

**仍缺**：

1. 更完整的搜索结果详情；
2. 更强的上下文呈现；
3. 更自然的页面流转。

## 3.3 Admin Frontend

### A1. 后台端仍然不是成熟比赛终态

**状态**：部分完成。

**证据**：

- `apps/frontend/admin/src/main.ts` 无 router；
- `apps/frontend/admin/src/App.vue` 单页承载登录/监控/审核/解析任务。

### A2. parse jobs 仍只有基础筛选与基础排障

**状态**：部分完成。

**证据**：

- 筛选仅 `status + entityType`：`App.vue:258-275`
- 详情仍以 JSON `pre` 为主：`App.vue:286-287`, `304-305`, `321-322`

**仍缺**：

1. 多维组合筛选；
2. 更结构化的任务详情；
3. 更强的失败原因聚合；
4. 更完整统计视图。

## 3.4 Test / QA

### T1. 浏览器级 E2E 已从 0 提升到首轮落地，但覆盖仍偏窄

**状态**：部分完成。

**已完成**：

- `tests/e2e/student-auth.spec.ts`
- `tests/e2e/admin-auth.spec.ts`

**证据**：

- `playwright.config.ts`
- `docs/67_浏览器级E2E首轮落地记录.md`

**仍缺**：

1. student 内容详情联动流；
2. admin parse-job retry；
3. admin review approve / reject / replace；
4. 更完整失败态 / 异常态浏览器验证；
5. README 与旧 docs 收口。

### T2. 回归体系还没有统一入口

**状态**：部分完成。

**现状**：

- smoke：`scripts/smoke_check.py`
- regression：`scripts/regression_check.py`
- browser E2E：根 `package.json` + Playwright

**仍缺**：

1. 一键执行的统一回归命令；
2. 更明确的回归矩阵；
3. 更稳定的环境切换方式。

## 3.5 Deploy / Ops

### D1. 公网统一入口 / 反向代理仍未完成

**状态**：未完成。

**现状**：

- backend：`:8000`
- student：`:3000`
- admin：`:3001`

**仍缺**：

1. Nginx / Caddy；
2. 统一入口；
3. 若要对外正式演示，需要更固定的访问方式。

> 用户当前明确说“绑定域名和反代先不做”，因此这是未完成项，但不是当前立刻推进项。

## 3.6 Docs

### D2. README 与部分历史文档仍然明显过时

**状态**：未完成。

**证据**：

1. 根 `README.md` 仍保留大量早期骨架阶段描述；
2. `apps/backend/README.md`、`apps/worker/README.md`、`apps/frontend/README.md` 与当前现实不一致；
3. `docs/32_下一阶段AI构建详细任务清单.md`、`docs/53_本地文件存储链路实现记录.md` 等仍带旧判断。

**仍缺**：

1. README 系列统一收口；
2. 对过时 docs 明确标注“已过期，以最新文档为准”；
3. 避免下一位 AI 继续被旧文档误导。

## 4. 当前最推荐推进顺序

基于当前真实状态，建议顺序如下：

1. 继续扩展浏览器级 E2E；
2. 再推进 student/admin 前端成品化；
3. 同步收口 README 与旧 docs；
4. 最后再考虑公网入口 / 反代。

## 5. 当前最高价值下一步

如果只继续推进一个最值当的事项，优先推荐：

### admin parse-job retry / review action 浏览器级 E2E

原因：

1. student 侧 auth / upload / search / solve 已基本覆盖；
2. 下一阶段最自然的测试扩展点已经转到 admin 真实操作流；
3. 这是当前 Playwright 覆盖里最明显的空白区。

## 6. 对下一位 AI 的提醒

不要再把“浏览器级 E2E”写成“完全没有”。

现在更准确的说法是：

> 浏览器级 E2E 已完成 student/admin 登录流首轮落地，但主业务流覆盖仍明显不足。
