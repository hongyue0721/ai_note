# 图谱服务计划

## 1. 目标

实现比赛级可用的图谱后端接口，让前端不再只是展示感知层，而是有真实数据源。

## 2. 本阶段最小交付

1. `/v1/graph/overview`
2. `/v1/graph/weak-tags`

## 3. 当前实现策略

比赛级阶段先基于现有数据聚合：

- `parse_job.result_json.knowledge_candidates`
- `problems`
- `notes`

## 4. 下一步

完成后接学生端图谱展示。
