# 内容更新接口实现记录

## 1. 本次目标

补齐内容更新接口。

## 2. 当前状态

当前已完成：

1. 增加 update schema；
2. 增加 PATCH route；
3. 接入现有 router。

## 3. 本次修改文件

- `apps/backend/app/schemas/content.py`
- `apps/backend/app/api/routes/problems.py`
- `apps/backend/app/api/routes/notes.py`

## 4. 当前说明

当前已补齐：

- `PATCH /v1/problems/{id}`
- `PATCH /v1/notes/{id}`
